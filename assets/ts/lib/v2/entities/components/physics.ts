import { MD2componentModule } from "../../../misc/components";
import { Entity } from "../entity";

export class EntityPhysicsComponent extends MD2componentModule<Entity> {
    onTick(t) {
        
    }
}